@AGENTS.md

# Claude Instructions

`AGENTS.md` is the canonical operating contract for this repository. Load it first, then use the matching files in `.agents/harness/personas/`, `.agents/harness/protocol/`, and `.agents/workflows/`.

Do not duplicate or override the rules here. If a task requires implementation, planning, testing, mobile UX, performance, Expo SDK 56 behavior, or legacy parity, follow the Harness process defined in `AGENTS.md`.
